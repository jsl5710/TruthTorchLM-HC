[pytest]
testpaths = tests
addopts = -m "not network"
markers =
    network: hits the HuggingFace Hub to confirm upstream dataset schemas have not
        drifted. Excluded by default so the suite stays fast and offline-safe; run
        them explicitly with `pytest -m network`.
